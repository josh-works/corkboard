require 'rails_helper'

#describe StripeService do
# it "charges a pro user given valid params" do
#   bid = create(:bid)
#   charge = StripeService.charge(pro_params, charge_params)

#   expect(response.status).to eq(200)
 #  expect(page).to have_content("Payment successful. Your bid was successfully submitted.")
 #  expect 
 #end
#end
